import React from 'react';
import { Box } from '@mui/material';

const CustomMobileMenuDivider = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        padding: '8px 16px',
      }}
    >
    </Box>
  );
};

export default CustomMobileMenuDivider;